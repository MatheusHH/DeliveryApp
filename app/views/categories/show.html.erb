<div class="card shadow p-3 mb-5">
  <div class="card-header">Category Details </div>
  <div class="card-body">

    <p>
      <strong><%= t('model_helpers.category.title') %>:</strong>
      <%= @category.title %>
    </p>


    <div class="card-footer">
    <div class="actions">
      <%= link_to categories_path, class: "btn btn-info btn-icon-split btn-sm" do %>
          <i class="fas fa-backward"></i>
          <span class="text"> Back</span>
        <% end %>
    </div>
    </div>
  </div>
</div>
